const Discord = require('discord.js');
const { Client, MessageAttachment } = require('discord.js');
module.exports= {
    name: 'work',
    cooldown: 60 * 300,
    description: "this is a work currency command!",
    execute(message, args){
        
    }
}